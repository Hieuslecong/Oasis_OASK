# Experiment artifacts

Training checkpoints and run outputs are intentionally excluded from Git. Store
official runs under the configured experiment root (for example
`/hdd1/hieulc/Oasis_AOSK/experiments/`) and record their hashes in the run
metadata. Historical debug checkpoints are not valid evidence for implementation
2.0.4.
