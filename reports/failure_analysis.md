# Failure analysis

Real failure artifacts are not generated because the real manifest paths and
DP-GAN checkpoint are unavailable. The framework reserves categories for
false positives, false negatives, topology breaks, thin cracks, shadow and
texture failures. Toy artifacts must not be mixed into final scientific
reports.
